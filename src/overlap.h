#ifndef OVERLAP_H
#define OVERLAP_H

#include <stdio.h>
#include <stdlib.h>
#include <string>

using namespace std;

class Overlap{
public:
    Overlap();

public:
    int mOffset;
    int mOverlapLen;
    int mDistance;
};

#endif